apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels:
    service: web
spec:
  replicas: 1
  selector:
    matchLabels:
      service: web
  template:
    metadata:
      labels:
        service: web
    spec:
      containers:
      - name: web
        image: containerregistryrobots.azurecr.io/web:v1.0.0
        ports:
        - containerPort: 8080
        env:
        - name: CATALOGUE_HOST
          value: catalogue
        - name: USER_HOST
          value: user
        - name: CART_HOST
          value: cart
        - name: SHIPPING_HOST
          value: shipping
        - name: PAYMENT_HOST
          value: payment
        - name: RATINGS_HOST
          value: ratings

        resources:
          requests:
            cpu: 50m
            memory: 100Mi
          limits:
            cpu: 300m
            memory: 200Mi

        livenessProbe:
          httpGet:
            path: /nginx_status
            port: 8080

        readinessProbe:
          httpGet:
            path: /nginx_status
            port: 8080