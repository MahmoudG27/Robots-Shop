apiVersion: batch/v1
kind: Job
metadata:
  name: mongo-init
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: mongo-init
          image: mongo:7.0
          env:
            - name: MONGO_USER
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: MONGO_INITDB_ROOT_USERNAME
            - name: MONGO_PASS
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: MONGO_INITDB_ROOT_PASSWORD
          command:
            - sh
            - -c
            - |
              echo "Waiting for MongoDB..."
              until mongosh --host mongodb:27017 -u $MONGO_USER -p $MONGO_PASS --authenticationDatabase admin --eval "db.adminCommand('ping')" >/dev/null 2>&1; do
                sleep 2
              done

              echo "Running init scripts..."
              mongosh --host mongodb:27017 -u $MONGO_USER -p $MONGO_PASS --authenticationDatabase admin /scripts/users.js
              mongosh --host mongodb:27017 -u $MONGO_USER -p $MONGO_PASS --authenticationDatabase admin /scripts/catalogue.js

              echo "Mongo init completed"
          volumeMounts:
            - name: scripts
              mountPath: /scripts
      volumes:
        - name: scripts
          configMap:
            name: mongo-init-scripts