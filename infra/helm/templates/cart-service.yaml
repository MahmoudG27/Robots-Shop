apiVersion: v1
kind: Service
metadata:
  name: cart
  labels:
    app: cart
spec:
  selector:
    app: cart
  ports:
  - name: http
    port: {{ .Values.cart.service.port }}
    targetPort: {{ .Values.cart.service.port }}
  type: ClusterIP