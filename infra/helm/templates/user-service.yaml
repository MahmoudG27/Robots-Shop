apiVersion: v1
kind: Service
metadata:
  name: user
  labels:
    app: user
spec:
  selector:
    app: user
  ports:
  - name: http
    port: {{ .Values.user.service.port }}
    targetPort: {{ .Values.user.service.port }}
  type: ClusterIP