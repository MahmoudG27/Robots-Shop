apiVersion: v1
kind: Service
metadata:
  name: mongodb
  labels:
    app: mongodb
spec:
  clusterIP: None
  selector:
    app: mongodb
  ports:
  - name: mongo
    port: {{ .Values.databases.mongodb.service.port }}
    targetPort: {{ .Values.databases.mongodb.service.port }}