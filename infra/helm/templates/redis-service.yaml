apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
spec:
  ports:
  - name: redis
    port: {{ .Values.cache.redis.service.port }}
    targetPort: {{ .Values.cache.redis.service.port }}
  clusterIP: None
  selector:
    app: redis