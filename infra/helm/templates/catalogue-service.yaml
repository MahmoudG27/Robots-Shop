apiVersion: v1
kind: Service
metadata:
  name: catalogue
  labels:
    app: catalogue
spec:
  selector:
    app: catalogue
  ports:
  - name: http
    port: {{ .Values.catalogue.service.port }}
    targetPort: {{ .Values.catalogue.service.port }}
  type: ClusterIP