apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rabbitmq-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: rabbitmq
  endpoints:
  - port: metrics
    path: /metrics
    interval: 15s