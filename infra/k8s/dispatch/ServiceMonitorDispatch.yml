apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dispatch-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: dispatch
  endpoints:
  - port: http
    path: /metrics
    interval: 15s