apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: user
  endpoints:
  - port: http
    path: /metrics
    interval: 15s