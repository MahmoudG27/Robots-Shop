apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ratings-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: ratings
  endpoints:
  - port: http
    path: /metrics
    interval: 15s