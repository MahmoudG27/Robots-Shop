apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: catalogue-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: catalogue
  endpoints:
  - port: http
    path: /metrics
    interval: 15s